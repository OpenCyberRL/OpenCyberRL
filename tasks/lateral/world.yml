# tasks/lateral/world.yml
x-cyberl:
  agent: attacker
networks:
  edge:
  backend:
services:
  attacker:
    build: build/attacker
    networks: [edge]
  web:
    build: build/web
    networks: [edge, backend]
  internal:
    build: build/internal
    networks: [backend]
